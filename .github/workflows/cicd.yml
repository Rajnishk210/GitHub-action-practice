name: CICD

on:
  workflow_dispatch: 

jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: Clone the code
        run: echo "Clone the code"
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build the code
        run: echo "Docker build"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test the code
        run: echo "Run python code"
        
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy our code
        run: echo "Deploy your code"
